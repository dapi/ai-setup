services:
  db:
    image: postgres:18.1-alpine3.23
    volumes:
      - ./data/db/18.1:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      - POSTGRES_PASSWORD=password
