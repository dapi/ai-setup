# Report

## Общая оценка:

Реализация brief -> spec -> plan не заняло много времени, я действовал в паре с ИИ. Бриф генерировал я сам с ИИ, и дальше уже один ИИ в одной вкладке генерировал документ (слабая модель), а в другой вкладке сильная ИИ делала ревью (сильная модель). Результата ревью отдавал слабой модели чтобы исправляла. Я также самостоятельно перечитывал чтобы документ соответствовал моему виденью. В целом на brief -> spec -> plan уходило не больше 10 минут. 

Единственное, что я заметил что лучше делать постепенно, сначала реализовать первый цикл brief -> spec -> plan -> implement, потом следующий такой план. Это лучше для этапа grounding + возможность ссылаться на детали реализации прошлых фич.

Для реализации использовал sonnet, а для ревью реализации opus. Пробовал Heiku, Heiku хороша тем, что некоторые детали недоудмывает себя, а выполняет буквально, и по итогу некоторые вещи остаются пустыми, то что в принципе sonnet или opus сами могут додумать.

## 007-configure-ci-cd

Начал с этой фичи, чтобы настроить сначала CI/CD на проекте.

Первый прогон реализации занял: 2:20 минут с помощью Sonnet

Review с помощью Opus заняло 1 минута + 2 минуты исправления

Что не доделано и что возможно я опустил:
- ИИ агент не создал PR для этой фичи

Наш ci-app.sh и новый app job отработали корректно — bin/ci нашёл реальные RuboCop нарушения в существующем коде. Bundler Audit и Brakeman  прошли.

Исправление стиля редактирования файлов заняло в несколько итераций, каждый раз когда падал один из воркфлоу, я просил ИИ агента посмотреть логи и исправить. ВСе это заняло в поярдке 4 итераций.

И того, с учетом реалзации, исправления и получение фидбека от CI воркфлоу, заняло порядка 30-40 минут.

## 002-forgot-password

Время работы агента (Sonnet): 59:09 минут. Давольно таки много, может быть Opus справился бы быстрее. Также не увидел скриншотов, поэтому я еще раз прогнал через /review с использованием Opus. Это заняло 12 минут, при этом Opus сделал нужные скришоты, на которых я увидел неточности. И Попросил на этот раз модель Heiku исправить, заняло ~5 минут на 2 итерации. 

Итого: 1 час 16 минут.

