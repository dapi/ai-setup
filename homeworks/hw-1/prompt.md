# Feature #001. Add Linter & Formatter - https://github.com/akoltun/ai-setup/issues/1

## Brief
### Prompt #1
Read the issue 1 from the repository of the project.
Generate the Brief in accord with SDD best practices
Save it to `memory-bank/features/001/brief.md`

### Prompt #2
Read the Breif from `memory-bank/features/001/brief.md`
Pay attention on Open Questions at the end.
Adjust the Brief in accord with the following answers for those questions:
1. Check the documentation: https://oxc.rs/docs/guide/usage/formatter.html#oxfmt. Find and install plugin if needed.
2. Yes, include `.json` and `.md`
3. No

### Prompt #3 - повторялся в новой сессии многократно
Ты — ревьюер бизнес-задач. Проверь Brief в `memory-bank/features/001/brief.md` на полноту и однозначность.

Критерии:
1. Понятен контекст: откуда задача, почему важна сейчас
2. Brief НЕ содержит решения — только проблему и желаемый результат
3. Нет двусмысленных формулировок («быстро», «удобно», «при необходимости»)

Для каждого найденного замечания укажи:
- Что именно не так (цитата из документа)
- Почему это проблема
- Как исправить (конкретное предложение)

Если замечаний нет — напиши «0 замечаний, Brief готов к работе».

### Prompt #4 (в сессии Prompt-а #3)
Исправь замечания в соостветствие с предложенным.

В отношение замечания 2. Необходимо утолчнить, что после первого запуска линтера могут появиться ошибки, требующие ручного исправления. Для завершения задачи, все ошибки должны быть исправлены, и итоговый запуск линтера не должен находить ошибок.

### Prompt #5 (в сессии Prompt-а #3)
Примени исправления. Касательно второго замечания - это стандартное поведение 

### Prompt #6 (в сессии Prompt-а #3)
примени исправления, не создавай Spec.md

### Prompt #7 (в сессии Prompt-а #3)
примени исправления, не создавай Spec.md

### Prompt #8 (в сессии Prompt-а #3)
примени исправления, не создавай Spec.md

### Prompt #9 (в сессии Prompt-а #3)
Исправь замечания: убери Constraints, явно перечисли поддерживаемые линтером и форматтером типы файлов. Поддерживаемые типы файлов, определи из документации                             
  https://oxc.rs/docs/guide/usage/formatter.html#oxfmt и https://oxc.rs/docs/guide/usage/linter.html#oxlint  

### Prompt #10 (в сессии Prompt-а #3)
примени предложенные исправления, warnings не допустимы

## Spec
### Prompt #11
Ты работаешь по SDD.
Прочитай Brief из `memory-bank/features/001/brief.md` и сгенерируй Spec в `memory-bank/features/001/spec.md`.

### Prompt #12
Скорректируй Spec `memory-bank/features/001/spec.md` следующим образом:
- Используй конфигурационные файлы в формате TypeScript, а не json
- Исключения описывай в конфигурационном файле, а не в ignore-файле
- Явно укажи в исключениях:
  - все папки начинающиеся с точки
  - `homeworks/`
  - `node_modules/`
  - `dist`

### Prompt #13
Скорректируй Spec `memory-bank/features/001/spec.md` следующим образом:
- Настрой line width limit равным 120 символам
- Настрой сортировку импорта и `package.json` файла
- Добавь в конфигурацию линтера plugins: `react`, `import`, `promise`, `jest`
- Используй plugin-ы только для соответствующих файлов, например:
  - jest - только для тестов внутри папки `src`
  - react - только для frontend приложения

### Prompt #14
Скорректируй Spec `memory-bank/features/001/spec.md` следующим образом:
- Используй type-aware linting

### Prompt #15 - review - повторялся в новой сессии многократно
Ты — строгий ревьюер спецификаций для AI-агентов. Проверь спеку `memory-bank/features/001/spec.md` по критериям TAUS.

Для каждого критерия дай оценку pass/fail:

1. Testable — есть ли конкретные acceptance criteria, по которым можно написать автотест?
2. Ambiguous-free — есть ли двусмысленные слова («быстро», «удобно», «при необходимости», «и т.д.»)?
3. Uniform — описаны ли все состояния (loading, error, success, empty)? Все сценарии ошибок?
4. Scoped — это одна фича? Меньше 1500 слов? Не затрагивает больше 3 модулей?

Дополнительно проверь:
5. Есть ли ссылка на Brief/Issue?
6. Указан ли scope (что входит И что НЕ входит)?
7. Перечислены ли инварианты?
8. Указаны ли ограничения на реализацию?

Для каждого fail:
- Цитата из спеки
- Почему это проблема для агента
- Конкретное предложение по исправлению

Если все критерии pass — напиши «0 замечаний, спека готова к реализации».

### Prompt #16 (в сессии Prompt-а #15)
Примени предложенные исправления.
По замечанию 3 - используй вариант с ручным исправлением.

### Prompt #17 (в сессии Prompt-а #15)
Примени предложенные исправления.

### Prompt #18 (в сессии Prompt-а #15)
Примени предложенные исправления.

### Prompt #19 (в сессии Prompt-а #15)
Примени предложенные исправления пунктов 1 и 3.

### Prompt #20 (в сессии Prompt-а #15)
Примени предложенные исправления из пунктов 2 и 3.

### Prompt #21 (в сессии Prompt-а #15)
Примени предложенные исправления.

### Prompt #22 (в сессии Prompt-а #15)
Примени предложенные исправления.

### Prompt #23 (в сессии Prompt-а #15)
Примени предложенное исправление по замечанию 1, используй вариант с проверкой файловой системы

### Prompt #24
Скорректируй Spec `memory-bank/features/001/spec.md` следующим образом:
- путем изменения конфигурации oxlint (секции `ignorePatterns`) исключи из линтинга все кроме содержимого `src/` и `tests/`
- облась применения форматтера не меняется
- проверь все пункты файла на согласованность с новым изменением, и при необходимости подкорректируй их

### повторить Prompt #15 - review

## Plan
### Prompt #25
Ты работаешь по SDD.
Прочитай Spec в `memory-bank/features/001/spec.md` и сгенерируй Plan в `memory-bank/features/001/plan.md`.

### Prompt #26
Ты работаешь по SDD.
Прочитай Plan из `memory-bank/features/001/plan.md`.
Проверь этот план на совместимость с текущей кодовой базой. Какие файлы затронешь? Есть ли конфликты? Осуществимо ли это?

### Prompt #27
Ты — ревьюер планов реализации. Проверь Plan в `memory-bank/features/001/plan.md` на выполнимость и корректность.

Критерии:
1. Каждый шаг конкретен — понятно, какой файл затрагивается и что именно меняется
2. Порядок шагов корректен — зависимости не нарушены, нет циклов
3. Каждый шаг атомарен — можно выполнить и проверить независимо
4. Нет пропущенных шагов — например, миграции, обновление тестов, обновление документации
5. Grounding: упомянутые файлы и модули существуют в проекте

Для каждого найденного замечания:
- Какой шаг затронут
- Почему это проблема
- Как исправить

Если замечаний нет — напиши «0 замечаний, план готов к реализации».

### Prompt #28 (в сессии Prompt-а #27)
Примени предложенные исправления.

### Prompt #29 (в сессии Prompt-а #27)
Примени предложенные исправления.
По Замечанию 2, используй вариант с примечанием

## Correction of Spec and Plan
### Prompt #30
Скорректируй Spec `memory-bank/features/001/spec.md` и Plan `memory-bank/features/001/plan.md`.

Помимо oxlint и oxfmt, необходимо также установить пакет oxlint-tsgolint. Он нужен для работе type-aware линтера.

## Implementation
### Prompt #31
Ты principal developer работающий по SDD.
Реализуй по спеке `memory-bank/features/001/spec.md` в соответствие с планом `memory-bank/features/001/plan.md`.

## Verify
### Prompt #32 - см. файл .ai-setup/prompts/deep-code-review.md

### Prompt #33
Исправить замечания:
  1. [P1-003] + [P1-004] — Исправить логику NODE_ENV и добавить его в dev-скрипт. Это блокирует безопасный деплой.
  2. [P1-007] — Добавить "лишние" правила из oxlint.config.ts в спеку. Это закрывает Feature 001.                                                                         
  3. [P1-001] — Добавить error handling в fetchAppeal (try/catch + res.ok). Это блокирует подключение реальных данных.                                                                     
  4. [P1-002] — Выключить non-null assertion проверку в `frontend.tsx`.tsx.                                                                                                              
  5. [P1-006] — Игнорировать - будет сделано в отдельном задании
  6. [P1-008] — Игнорировать - это sample endpoint, будет удален при добавлении первой реально фичи
  7. [P1-005] — Зафиксировать @types/bun версию. Один символ в package.json. 

# Feature #002. Add tests - https://github.com/akoltun/ai-setup/issues/2
### Prompt #34
Прочитай issue 2 из репозитория проекта
Сгенерируй Brief в соответствие  SDD best practices
Сохрани его в `memory-bank/features/002/brief.md`

### Prompt #35
Ты — ревьюер бизнес-задач. Проверь Brief в `memory-bank/features/002/brief.md` на полноту и однозначность.

Критерии:
1. Понятен контекст: откуда задача, почему важна сейчас
2. Brief НЕ содержит решения — только проблему и желаемый результат
3. Нет двусмысленных формулировок («быстро», «удобно», «при необходимости»)

Для каждого найденного замечания укажи:
- Что именно не так (цитата из документа)
- Почему это проблема
- Как исправить (конкретное предложение)

Если замечаний нет — напиши «0 замечаний, Brief готов к работе».

### Prompt #36
примени исправления, не создавай Spec.md

### Prompt #37
примени исправления, не создавай Spec.md

## Spec
### Prompt #38
Ты работаешь по SDD.
Прочитай Brief из `memory-bank/features/002/brief.md` и сгенерируй Spec в `memory-bank/features/002/spec.md`.

### Prompt #39
Ты работаешь по SDD.
Прочитай Spec из `memory-bank/features/002/spec.md`.
Скорректируй Spec:
- Скрипты `test:api` и `test:e2e` должны тестировать приложение в development режиме
- Скрипт `test` должен тестировать приложение в production режиме. Перед запуском тестов он должен выполнить build
- Используй именной экспорт сервера, а на default (следуй conventions из `CLAUDE.md`)

### Prompt #40 - review - повторялся в новой сессии многократно
Ты — строгий ревьюер спецификаций для AI-агентов. Проверь спеку `memory-bank/features/002/spec.md` по критериям TAUS.

Для каждого критерия дай оценку pass/fail:

1. Testable — есть ли конкретные acceptance criteria, по которым можно написать автотест?
2. Ambiguous-free — есть ли двусмысленные слова («быстро», «удобно», «при необходимости», «и т.д.»)?
3. Uniform — описаны ли все состояния (loading, error, success, empty)? Все сценарии ошибок?
4. Scoped — это одна фича? Меньше 1500 слов? Не затрагивает больше 3 модулей?

Дополнительно проверь:
5. Есть ли ссылка на Brief/Issue?
6. Указан ли scope (что входит И что НЕ входит)?
7. Перечислены ли инварианты?
8. Указаны ли ограничения на реализацию?

Для каждого fail:
- Цитата из спеки
- Почему это проблема для агента
- Конкретное предложение по исправлению

Если все критерии pass — напиши «0 замечаний, спека готова к реализации».

## Plan
### Prompt #41
Ты работаешь по SDD.
Прочитай Spec в `memory-bank/features/002/spec.md` и сгенерируй Plan в `memory-bank/features/002/plan.md`.

### Prompt #42
Скорректируй Spec `memory-bank/features/002/spec.md` и Plan `memory-bank/features/002/plan.md`:
- Установи `retries` в конфигурации Playwright в значение `process.env.CI ? 2 : 0
- Установи `workers` в конфигурации Playwright в значение `process.env.CI ? 1 : undefined
- Скорректируй соответствующие описания
- Папки, добавляемые в `.gitignore`, должны быть также внесены в игнор-списки в `oxfmt.config.ts`. Поскольку `oxlint.config.ts` уже исключает их, то его изменять не надо

### Prompt #43
Ты — ревьюер планов реализации. Проверь Plan в `memory-bank/features/002/plan.md` на выполнимость и корректность.

Критерии:
1. Каждый шаг конкретен — понятно, какой файл затрагивается и что именно меняется
2. Порядок шагов корректен — зависимости не нарушены, нет циклов
3. Каждый шаг атомарен — можно выполнить и проверить независимо
4. Нет пропущенных шагов — например, миграции, обновление тестов, обновление документации
5. Grounding: упомянутые файлы и модули существуют в проекте

Для каждого найденного замечания:
- Какой шаг затронут
- Почему это проблема
- Как исправить

Если замечаний нет — напиши «0 замечаний, план готов к реализации».

### Prompt #44 (в сессии Prompt-а #43)
Примени предложенные исправления.
По Замечанию 5 - скорректируй Spec и Plan, чтобы добавить `dist/` в `.gitignore`

## Implementation
### Prompt #45
Ты principal developer работающий по SDD.
Реализуй по спеке `memory-bank/features/002/spec.md` в соответствие с планом `memory-bank/features/002/plan.md`.

## Verify
### Prompt 46
Ты — строгий code reviewer. Нужен semi-formal reasoning по изменению.
Не давай общих фраз. Каждое утверждение подкрепляй ссылкой на конкретный код/дифф.

Контекст -
- Цель изменения: Возьми из `memory-bank/features/002/spec.md`
- Ограничения: Возьми из `memory-bank/features/002/spec.md`
- Дифф/файлы: Возьми из `git diff HEAD~5 HEAD`

Выведи ответ строго в формате:

1) Предпосылки
- Явные допущения, без которых выводы недействительны.

2) Инварианты и контракты
- Какие свойства системы должны сохраняться после изменения.

3) Трассировка путей выполнения
- Ключевые happy-path и error-path.
- Где поведение изменилось, где не изменилось.

4) Риски и регрессии
- Список рисков (severity: high/medium/low).
- Для каждого: почему риск реален и где в коде это видно.

5) Вердикт по эквивалентности поведения
- Эквивалентно / Неэквивалентно / Недостаточно данных.
- Если неэквивалентно: минимальный контрпример (вход -> ожидаемое расхождение).

6) Что проверить тестами
- Топ-5 проверок, которые закроют неопределённость.

7) Confidence
- Оценка 0..1 и коротко: что мешает дать более высокий confidence.

Правила:
- Не придумывай факты, которых нет в диффе/коде.
- Если данных не хватает, явно помечай это как "Недостаточно данных".
- Пиши кратко и по делу.

### Prompt 47
В `memory-bank/features/002/`:
- обнови спеку и план, чтобы они соответствовали фактическому `playwright.config.ts`, который берет порт из `process.env.PORT`
- в `src/index.ts` используй `export const server =` вместо `export { server }`
- в `package.json` приведи скрипты запуска тестов в соответствие со спекой (используй `bun test` вместо `bun test src/`)

# Feature #003. Add CI/CD - https://github.com/akoltun/ai-setup/issues/3
## Brief
### Prompt #34
Прочитай issue 3 из репозитория проекта
Сгенерируй brief в соответствие с SDD best practices
Сохрани его в `memory-bank/features/003/brief.md`

Не включай в Brief детали реализации. Brief должен описывать результат, а не то, как его получить.

### Prompt #35
Ты — ревьюер бизнес-задач. Проверь Brief в `memory-bank/features/003/brief.md` на полноту и однозначность.

Критерии:
1. Проблема конкретна и измерима (не «улучшить», а конкретная метрика или боль)
2. Понятен контекст: откуда задача, почему важна сейчас
3. Brief НЕ содержит решения — только проблему и желаемый результат
4. Нет двусмысленных формулировок («быстро», «удобно», «при необходимости»)

Для каждого найденного замечания укажи:
- Что именно не так (цитата из документа)
- Почему это проблема
- Как исправить (конкретное предложение)

Если замечаний нет — напиши «0 замечаний, Brief готов к работе».

## Spec
### Prompt #41
Ты работаешь по SDD.
Прочитай Brief из `memory-bank/features/003/brief.md` и сгенерируй Spec в `memory-bank/features/003/spec.md`.

### Prompt #42
Скорректируй `memory-bank/features/003/spec.md`.
- Добавь джоб, который подготавливает среду и устанавливает все завасимости. Остальные джобы от него зависят
- Не просто добавь описание проекта в README, а актуализируй все разделы README в соответствие с текущим состоянием проекта 

### Prompt #43
Скорректируй `memory-bank/features/003/spec.md`.
- В разделе Installation уточни, что база данных, и все относящееся к ней, включая скрипты в `package.json` пока не добавлены в проект, поэтому их установка не требуется

### Prompt #44 - review - повторялся в новой сессии многократно
Ты — строгий ревьюер спецификаций для AI-агентов. Проверь спеку `memory-bank/features/003/spec.md` по критериям TAUS.

Для каждого критерия дай оценку pass/fail:

1. Testable — есть ли конкретные acceptance criteria, по которым можно написать автотест?
2. Ambiguous-free — есть ли двусмысленные слова («быстро», «удобно», «при необходимости», «и т.д.»)?
3. Uniform — описаны ли все состояния (loading, error, success, empty)? Все сценарии ошибок?
4. Scoped — это одна фича? Меньше 1500 слов? Не затрагивает больше 3 модулей?

Дополнительно проверь:
5. Есть ли ссылка на Brief/Issue?
6. Указан ли scope (что входит И что НЕ входит)?
7. Перечислены ли инварианты?
8. Указаны ли ограничения на реализацию?

Для каждого fail:
- Цитата из спеки
- Почему это проблема для агента
- Конкретное предложение по исправлению

Если все критерии pass — напиши «0 замечаний, спека готова к реализации».

### Prompt #45 - в сессии Prompt-а #44
Примени предложенные исправления в `memory-bank/features/003/spec.md`
- Для Проблемы 2 из Раздела 1 Testable помимо применения предложенного исправления, из текущего текста сделай объяснение, почему порог выставляется по текущему значению, а не в 80% согласно требованию Brief-а
- Для Проблемы 1 из Раздела 2 Ambiguous-free "Все разделы" означает, что агент должен прочитать README, и работать со всеми разделами, которые он там обнаружит. Они не должны перечисляться в спеке. "При необходимости" значит, что агент должен сравнить содержимое раздела с актуальным состоянием проекта, и если они не соответствуют друг другу, то переписать раздел исходя из актуального состояния проекта
- Все остальные исправления примени как они предложены
- Также добавь пункт, что по завершении работы агент должен вывести на экран пошаговую инструкцию для:
  - ручной настройки GitHub, необходимой для того, чтобы пайплайны заработали (настройка секретов GitHub и пр.)

### Prompt #46 - в сессии Prompt-а #44
Примени предложенные исправления в `memory-bank/features/003/spec.md`, кроме раздела Ambiguous-free

### Prompt #47 - review - повторялся в новой сессии многократно
Ты — строгий ревьюер спецификаций для AI-агентов. Проверь спеку `memory-bank/features/003/spec.md` по критериям TAUS.

Для каждого критерия дай оценку pass/fail:

1. Testable — есть ли конкретные acceptance criteria, по которым можно написать автотест?
2. Ambiguous-free — есть ли двусмысленные слова («быстро», «удобно», «при необходимости», «и т.д.»)?
3. Uniform — описаны ли все состояния (loading, error, success, empty)? Все сценарии ошибок?
4. Scoped — это одна фича? Меньше 1500 слов? Не затрагивает больше 3 модулей?

Дополнительно проверь:
5. Есть ли ссылка на Brief/Issue?
6. Указан ли scope (что входит И что НЕ входит)?
7. Перечислены ли инварианты?
8. Указаны ли ограничения на реализацию?

Для каждого fail:
- Цитата из спеки
- Почему это проблема для агента
- Конкретное предложение по исправлению

Игнорируй при проверке неоднозначность и субъективность определений относящихся к обновлению README файла

Если все критерии pass — напиши «0 замечаний, спека готова к реализации».